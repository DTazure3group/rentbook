apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway
  namespace: rentbook
  labels:
    app: gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway
  template:
    metadata:
      labels:
        app: gateway
    spec:
      containers:
        - name: gateway
          image: grp03.azurecr.io/gateway:latest
          ports:
            - containerPort: 8080